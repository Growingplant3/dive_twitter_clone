<h1>ツイート新規登録</h1>
<div>
  <%= form_with(model: @tweet, local: true) do |form| %>
    <p>
      <%= form.label :content %>
      <%= form.text_field :content %>
    </p>
    <%= form.submit "新規登録する" %>
  <% end %>
</div>
<%= link_to "ツイート一覧へ戻る", tweets_path %>
